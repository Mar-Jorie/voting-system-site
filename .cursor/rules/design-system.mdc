---
description: Core design system specifications that AI agents MUST follow for all React applications. Includes typography, spacing, colors, component heights, and visual specifications.
alwaysApply: true
---

# 🎨 Design System Rules (MANDATORY)

## ⚠️ CRITICAL: AI AGENTS MUST FOLLOW THESE RULES

**These design specifications are MANDATORY for all implementations. Deviating from these rules is NOT ALLOWED.**

---

## 🎯 Tailwind CSS v4 Setup (MANDATORY)

### CSS File Structure (MUST FOLLOW)
```css
/* Tailwind CSS v4 Setup - MANDATORY */
@import "tailwindcss";
/* Bootstrap Icons removed - use @heroicons/react instead */

@reference; /* CRITICAL: Required for Tailwind v4 when using @apply in custom classes */

@theme {
  --color-primary-25: #ffffff;
  --color-primary-50: #e9eef2;
  --color-primary-100: #d3dde5;
  --color-primary-200: #bdcdd8;
  --color-primary-300: #a7bccb;
  --color-primary-400: #91abbe;
  --color-primary-500: #7b9ab1;
  --color-primary-600: #6589a4; /* Main brand color */
  --color-primary-700: #4f7897;
  --color-primary-800: #39678a;
  --color-primary-900: #23577d;
  --color-primary-950: #1a4566;
}

:root {
  /* CSS Variables for backwards compatibility */
  --color-primary-25: #ffffff;
  --color-primary-50: #e9eef2;
  --color-primary-100: #d3dde5;
  --color-primary-200: #bdcdd8;
  --color-primary-300: #a7bccb;
  --color-primary-400: #91abbe;
  --color-primary-500: #7b9ab1;
  --color-primary-600: #6589a4; /* Main brand color */
  --color-primary-700: #4f7897;
  --color-primary-800: #39678a;
  --color-primary-900: #23577d;
  --color-primary-950: #1a4566;
}
```

### Tailwind v4 Requirements (MANDATORY)
```css
/* CRITICAL REQUIREMENTS - MANDATORY */
1. ALWAYS use @import "tailwindcss" (not @tailwind directives)
2. ALWAYS include @reference directive when using @apply in custom classes
3. ALWAYS define colors in @theme directive for Tailwind v4
4. ALWAYS maintain :root CSS variables for backwards compatibility
5. NEVER use tailwind.config.js with Tailwind v4 (use @theme instead)
```

### Custom Component Styles (MUST USE)
```css
/* Custom Component Styles - MANDATORY */
.modern-nav-item {
  @apply flex items-center space-x-3 px-4 py-3 text-sm font-medium text-gray-600 rounded-lg transition-all duration-200 hover:bg-gray-50 hover:text-gray-900;
}

.modern-nav-item.active {
  @apply bg-primary-50 text-primary-700 border-r-2 border-primary-600;
}

.filter-select-input {
  @apply bg-white border-gray-200 text-gray-900;
}

.filter-select-input:focus {
  @apply border-primary-500 ring-primary-500;
}

.btn-primary {
  @apply bg-primary-600 text-white hover:bg-primary-700;
}

.btn-secondary {
  @apply bg-gray-600 text-white hover:bg-gray-700;
}

.card {
  @apply bg-white rounded-lg shadow-sm border border-gray-100;
}

.form-input {
  @apply w-full h-10 px-3 border border-gray-300 rounded-md text-sm transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent;
}

.form-input.error {
  @apply border-red-300 focus:ring-red-500;
}
```

---

## 📝 Typography System (MANDATORY)

### Text Sizes (MUST USE)
- **text-xs**: 12px (Captions, labels, metadata)
- **text-sm**: 14px (Body text, small content)
- **text-base**: 16px (Standard body text)
- **text-lg**: 18px (Large body text, descriptions)
- **text-xl**: 20px (Small headings, card titles)
- **text-2xl**: 24px (Section headings)
- **text-3xl**: 30px (Page headings)
- **text-4xl**: 36px (Hero headings)
- **text-5xl**: 48px (Display headings)

### Font Weights (MANDATORY)
- **font-normal**: 400 (Body text)
- **font-medium**: 500 (Emphasis, labels)
- **font-semibold**: 600 (Headings, important text)
- **font-bold**: 700 (Strong emphasis, hero text)

### Typography Usage Rules (MANDATORY)
- **Page Titles**: text-2xl md:text-3xl lg:text-4xl font-semibold text-gray-900 mb-2
- **Section Headers**: text-xl md:text-2xl lg:text-3xl font-semibold text-gray-900 mb-4
- **Card Titles**: text-lg md:text-xl lg:text-2xl font-semibold text-gray-900 mb-3
- **Body Text**: text-sm md:text-base lg:text-lg text-gray-600 leading-relaxed
- **Captions**: text-xs text-gray-500
- **CRITICAL**: NO 3xl: font size prefixes allowed - large desktop uses same font sizes as desktop

---

## 📐 Spacing System (MANDATORY)

### Spacing Scale (MUST USE)
```css
/* Spacing Scale - MANDATORY */
space-1: 4px    /* Micro spacing */
space-2: 8px    /* Small spacing */
space-3: 12px   /* Medium-small spacing */
space-4: 16px   /* Medium spacing */
space-6: 24px   /* Large spacing */
space-8: 32px   /* Extra large spacing */
space-12: 48px  /* Huge spacing */
```

### Component Spacing (MANDATORY)
```css
/* Card Padding - MANDATORY */
p-4: 16px    /* Small cards */
p-5: 20px    /* Metric cards */
p-6: 24px    /* Standard cards */
p-8: 32px    /* Large cards */

/* Section Spacing - MANDATORY */
mb-6: 24px   /* Section bottom margin */
space-y-4: 16px /* Vertical spacing between elements */
space-y-6: 24px /* Vertical spacing between sections */
gap-4: 16px  /* Grid gaps */
gap-6: 24px  /* Large grid gaps */
```

### Container Width Rules (MANDATORY)
```css
/* CRITICAL: NO max-width containers allowed */
/* Use full width with responsive padding instead */

/* CORRECT - Full width with responsive padding */
<div className="w-full px-4 sm:px-6 lg:px-6">
  {/* Content */}
</div>

/* INCORRECT - Max-width containers */
<div className="max-w-7xl mx-auto">  /* ❌ NOT ALLOWED */
<div className="max-w-6xl mx-auto">  /* ❌ NOT ALLOWED */
<div className="max-w-4xl mx-auto">  /* ❌ NOT ALLOWED */

/* Responsive Padding Pattern - MANDATORY */
px-4 sm:px-6 lg:px-6  /* Mobile: 16px, Tablet: 24px, Desktop: 16px */
```

---

## 🎨 Color System (MANDATORY)

### Primary Brand Colors (MUST USE)
```css
/* Primary Color Palette - MANDATORY (Brand Colors) */
primary-25: #ffffff
primary-50: #e9eef2
primary-100: #d3dde5
primary-200: #bdcdd8
primary-300: #a7bccb
primary-400: #91abbe
primary-500: #7b9ab1
primary-600: #6589a4  /* Main brand color */
primary-700: #4f7897
primary-800: #39678a
primary-900: #23577d
primary-950: #1a4566

/* CSS Variable Naming - REQUIRED */
:root {
  --color-primary-25: #ffffff;
  --color-primary-50: #e9eef2;
  --color-primary-100: #d3dde5;
  --color-primary-200: #bdcdd8;
  --color-primary-300: #a7bccb;
  --color-primary-400: #91abbe;
  --color-primary-500: #7b9ab1;
  --color-primary-600: #6589a4; /* Main brand color */
  --color-primary-700: #4f7897;
  --color-primary-800: #39678a;
  --color-primary-900: #23577d;
  --color-primary-950: #1a4566;
}
```

### Semantic Colors (MANDATORY)
```css
/* Success Colors - MANDATORY (Use green-* naming) */
green-50: #ecfdf5
green-100: #d1fae5
green-200: #a7f3d0
green-300: #6ee7b7
green-400: #34d399
green-500: #10b981
green-600: #059669
green-700: #047857
green-800: #065f46
green-900: #064e3b

/* Error Colors - MANDATORY (Use red-* naming) */
red-50: #fef2f2
red-100: #fee2e2
red-200: #fecaca
red-300: #fca5a5
red-400: #f87171
red-500: #ef4444
red-600: #dc2626
red-700: #b91c1c
red-800: #991b1b
red-900: #7f1d1d

/* Warning Colors - MANDATORY (Use orange-* naming) */
orange-50: #fff7ed
orange-100: #ffedd5
orange-200: #fed7aa
orange-300: #fdba74
orange-400: #fb923c
orange-500: #f97316
orange-600: #ea580c
orange-700: #c2410c
orange-800: #9a3412
orange-900: #7c2d12

/* Info Colors - MANDATORY (Use blue-* naming) */
blue-50: #eff6ff
blue-100: #dbeafe
blue-200: #bfdbfe
blue-300: #93c5fd
blue-400: #60a5fa
blue-500: #3b82f6
blue-600: #2563eb
blue-700: #1d4ed8
blue-800: #1e40af
blue-900: #1e3a8a
```

### Text Colors (MANDATORY)
```css
/* Text Color Hierarchy - MANDATORY */
text-gray-900: #111827  /* primary text, headings */
text-gray-600: #4b5563  /* Secondary text, body */
text-gray-500: #6b7280  /* Muted text, labels */
text-gray-400: #9ca3af  /* Disabled text, placeholders */
```

---

## 📏 Component Heights (MANDATORY)

### Button Heights (MUST USE)
```css
/* Button Heights - MANDATORY */
h-8: 32px     /* Small buttons (sm) */
h-10: 40px    /* Standard buttons (md) */
h-12: 48px    /* Large buttons (lg) */
h-14: 56px    /* Extra large buttons (xl) */
```

### Input Heights (MUST USE)
```css
/* Input Heights - MANDATORY */
h-10: 40px    /* Standard inputs */
h-12: 48px    /* Large inputs */
```

### Card Heights (MUST USE)
```css
/* Card Heights - MANDATORY */
min-h-[200px]: 200px  /* Minimum card height */
min-h-[300px]: 300px  /* Standard card height */
min-h-[400px]: 400px  /* Large card height */
```

---

## 🔲 Border Radius (MANDATORY)

### Radius Scale (MUST USE)
```css
/* Border Radius Scale - MANDATORY */
rounded-sm: 4px     /* Small elements, badges */
rounded-md: 6px     /* Buttons, inputs */
rounded-lg: 8px     /* Cards, containers */
rounded-xl: 12px    /* Large cards, modals */
rounded-2xl: 16px   /* Hero sections */
rounded-full: 50%   /* Avatars, pills */
```

### Component Radius (MANDATORY)
```css
/* Component Border Radius - MANDATORY */
/* Buttons */
rounded-md: 4px

/* Inputs */
rounded-md: 4px

/* Cards */
rounded-lg: 8px

/* Modals */
rounded-xl: 12px

/* Avatars */
rounded-full: 50%
```

---

## 🎯 Button System (MANDATORY)

### Button Variants (MUST USE)
```jsx
// Primary Button - MANDATORY (variant "primary" uses primary palette)
<Button variant="primary" size="md">Primary Action</Button>

// Secondary Button - MANDATORY
<Button variant="secondaryOutline" size="md">
  Secondary Action
</Button>

// Success Button - MANDATORY
<Button variant="success" size="md">
  Success Action
</Button>

// Danger Button - MANDATORY
<Button variant="danger" size="md">
  Delete Action
</Button>

// Ghost Button - MANDATORY
<Button variant="ghost" size="md">
  Utility Action
</Button>
```

### Button Sizes (MANDATORY)
```jsx
// Small Button
<Button size="sm" className="h-8 px-3 text-xs">
  Small
</Button>

// Medium Button (DEFAULT)
<Button size="md" className="h-10 px-4 text-sm">
  Medium
</Button>

// Large Button
<Button size="lg" className="h-12 px-6 text-base">
  Large
</Button>
```

---

## 📱 Input System (MANDATORY)

### Input Factory (MUST USE)
```jsx
// Standard Input - MANDATORY PATTERN
<InputFactory
  fieldName="fieldName"
  config={{
    type: 'String',
    label: 'Field Label',
    placeholder: 'Enter value',
    required: true
  }}
  value={value}
  onChange={handleChange}
  error={error}
/>

// Password Input - MANDATORY PATTERN
<InputFactory
  fieldName="password"
  config={{
    type: 'String',
    format: 'password',
    showPasswordToggle: true
  }}
  value={password}
  onChange={handleChange}
/>
```

### Input Styling (MANDATORY)
```css
/* Input Base Classes - MANDATORY */
w-full h-10 px-3 border rounded-md text-sm
transition-colors duration-200
focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent
border-gray-300 focus:ring-primary-500
```

---

## 🃏 Card System (MANDATORY)

### Standard Card (MUST USE)
```jsx
// Standard Card - MANDATORY PATTERN
<div className="bg-white rounded-lg p-6 shadow-sm border border-gray-100">
  <h3 className="text-lg font-semibold text-gray-900 mb-4">Card Title</h3>
  <p className="text-sm text-gray-600">Card content...</p>
</div>
```

### Metric Card (MUST USE)
```jsx
// Metric Card - MANDATORY PATTERN
<div className="bg-white rounded-lg p-5 shadow-sm border border-gray-100">
  <div className="flex items-center justify-between mb-3">
    <div className="w-10 h-10 bg-primary-50 rounded-lg flex items-center justify-center">
      <ChartBarIcon className="h-6 w-6 text-primary-600" />
    </div>
    <span className="text-xs font-medium text-green-800 bg-green-50 px-2 py-1 rounded-full">
      Status
    </span>
  </div>
  <h3 className="text-2xl font-bold text-gray-900 mb-1">Value</h3>
  <p className="text-sm text-gray-600">Description</p>
</div>
```

---

## 🎨 Visual Effects (MANDATORY)

### Shadows (MUST USE)
```css
/* Shadow Scale - MANDATORY */
shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05)
shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1)
shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1)
shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1)
```

### Transitions (MUST USE)
```css
/* Transition Classes - MANDATORY */
transition-all duration-200 ease-out
transition-colors duration-200
transition-transform duration-300
```

### Color Opacity Format (MUST USE)
```jsx
// Tailwind slash opacity syntax is REQUIRED
// Correct examples:
bg-black/50     // 50% black background
bg-white/80     // 80% white background
text-gray-900/80
from-primary-600/90 to-primary-600/90  // gradients with opacity

// Do NOT use legacy utilities
// Incorrect:
bg-opacity-50   // ❌ Not allowed
text-opacity-70 // ❌ Not allowed
```

---

## ⚠️ MANDATORY IMPLEMENTATION RULES

### 1. Typography Rules (MANDATORY)
- ✅ ALWAYS use the specified text sizes
- ✅ ALWAYS use the specified font weights
- ✅ ALWAYS follow the typography hierarchy patterns
- ❌ NEVER use custom font sizes outside the scale
- ❌ NEVER use custom font weights outside the scale

### 2. Spacing Rules (MANDATORY)
- ✅ ALWAYS use the spacing scale values
- ✅ ALWAYS use consistent padding for similar components
- ✅ ALWAYS maintain proper spacing rhythm
- ❌ NEVER use arbitrary spacing values
- ❌ NEVER skip spacing between related elements

### 3. Color Rules (MANDATORY)
- ✅ ALWAYS use the specified color palette
- ✅ ALWAYS use semantic colors for their intended purpose
- ✅ ALWAYS follow the text color hierarchy
- ❌ NEVER use hard-coded colors outside the palette
- ❌ NEVER use colors for non-semantic purposes

### 4. Component Rules (MANDATORY)
- ✅ ALWAYS use the specified component heights
- ✅ ALWAYS use the specified border radius values
- ✅ ALWAYS follow the button and input patterns
- ❌ NEVER create custom component sizes
- ❌ NEVER deviate from the established patterns

### 5. Visual Consistency (MANDATORY)
- ✅ ALWAYS use the specified shadow values
- ✅ ALWAYS use the specified transition classes
- ✅ ALWAYS maintain visual consistency across components
- ❌ NEVER use custom shadow values
- ❌ NEVER skip transition effects on interactive elements

---

## 🚨 COMPLIANCE ENFORCEMENT

**AI AGENTS MUST:**
1. **Check every component** against these rules
2. **Validate typography** usage in all text elements
3. **Verify spacing** consistency across all layouts
4. **Confirm color** usage follows the semantic system
5. **Ensure component** heights and radius match specifications
6. **Validate button** and input patterns are followed

**NON-COMPLIANCE WILL RESULT IN:**
- ❌ Rejection of implementation
- ❌ Mandatory correction before proceeding
- ❌ Design system violation warnings

**THESE RULES ARE NON-NEGOTIABLE AND MUST BE FOLLOWED IN ALL IMPLEMENTATIONS.**